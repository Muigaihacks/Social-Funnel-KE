NEXT_PUBLIC_API_URL=http://localhost:4000
# Optional override for server-side fetches in Next (defaults to NEXT_PUBLIC_API_URL)
# BACKEND_URL=http://localhost:4000

# Create this file in apps/frontend as .env.local (gitignored) — not committed.
